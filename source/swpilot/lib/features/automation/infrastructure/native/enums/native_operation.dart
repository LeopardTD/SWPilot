/// Enum representing operations executed via the native automation bridge.
enum NativeOperation {
  initialize,
  checkPermission,
  checkAccessibility,
  startAutomation,
  stopAutomation,
  executeAction,
  getPlatformInfo,
}
